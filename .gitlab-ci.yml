stages:
  - test

unit-test:
  stage: test
  image: node:20-alpine
  script:
    - cd agent-harness
    - npm ci
    - npm test
    - cd ../NJU-Date-basic/backend
    - npm ci
    - npm run lint
    - npm test
  variables:
    NODE_ENV: test
    HARNESS_NETWORK: deny
