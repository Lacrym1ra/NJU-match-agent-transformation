# 03 单元测试报告

> **复测日期：** 2026-06-17  
> **测试对象：** `g3-main` / `origin/g3-main`  
> **测试提交：** `453482e` - Merge pull request #142 from software-engineering2/feature/g3-private-message  
> **测试范围：** 后端单元测试、前后端 TypeScript 静态检查、前端生产构建  
> **结论：** 本轮标准化单元测试与静态检查全部通过

---

## 一、测试目标

本轮按最新 `g3-main` 对可自动化复现的单元级质量门禁进行标准化复测，重点确认：

1. 后端已有 Node Test 单元用例全部通过；
2. 后端 TypeScript 类型检查通过；
3. 前端 TypeScript 类型检查通过；
4. 前端生产构建通过；
5. 形成可复查的执行记录，支撑 P4 阶段交付。

---

## 二、测试环境

| 项目 | 内容 |
|------|------|
| 仓库路径 | `C:\A\NJU-Date` |
| 当前分支 | `g3-main` |
| 远端同步 | `g3-main` 与 `origin/g3-main` 均为 `453482e` |
| Node/NPM | 使用本机 Node.js 与 `C:\Program Files\nodejs\npm.cmd` |
| 执行日期 | 2026-06-17 |

---

## 三、执行命令与结果

| 检查项 | 执行目录 | 命令 | 结果 | 关键输出 |
|--------|----------|------|------|----------|
| 后端类型检查 | `backend` | `& 'C:\Program Files\nodejs\npm.cmd' run lint` | 通过 | `tsc --noEmit` 完成 |
| 后端单元测试 | `backend` | `& 'C:\Program Files\nodejs\npm.cmd' test` | 通过 | `tests 10`, `pass 10` |
| 前端类型检查 | `frontend` | `& 'C:\Program Files\nodejs\npm.cmd' run lint` | 通过 | `tsc --noEmit` 完成 |
| 前端生产构建 | `frontend` | `& 'C:\Program Files\nodejs\npm.cmd' run build` | 通过 | Vite 构建完成，`built in 16.14s` |

---

## 四、后端单元测试明细

| 测试文件 | 用例数 | 本轮结果 | 覆盖内容 |
|----------|--------|----------|----------|
| `backend/src/services/heartboxPolicy.test.ts` | 3 | 通过 | 活跃匹配状态枚举、活跃状态判断、24 小时心动限制 |
| `backend/src/utils/studentId.test.ts` | 7 | 通过 | 学号格式校验、规范化、脱敏、哈希稳定性、邮箱/学号绑定策略 |
| 合计 | 10 | 全部通过 | 现有自动化单元测试均完成 |

### 4.1 `heartboxPolicy.test.ts`

| 用例 | 结果 |
|------|------|
| `ACTIVE_MATCH_STATUSES remains expected contract` | 通过 |
| `isActiveMainlineStatus accepts only active statuses` | 通过 |
| `canSetSignalIn24h enforces one-change-per-day gate` | 通过 |

### 4.2 `studentId.test.ts`

| 用例 | 结果 |
|------|------|
| `isValidStudentId validates undergraduate and postgraduate ids` | 通过 |
| `normalizeStudentId trims whitespace` | 通过 |
| `maskStudentId returns expected masked output` | 通过 |
| `hashStudentId is stable and opaque` | 通过 |
| `canonicalStudentIdFromEmail only accepts canonical local-part` | 通过 |
| `shouldAutoBindByEmail requires exact canonical match` | 通过 |
| `canonicalStudentEmailFromId builds target bind mailbox` | 通过 |

---

## 五、前端检查与构建记录

| 检查项 | 覆盖范围 | 本轮结果 |
|--------|----------|----------|
| TypeScript 静态检查 | `frontend/src`、`frontend/tests`、项目 TS 配置 | 通过 |
| Vite 生产构建 | React 页面、组件、API 封装、静态资源打包 | 通过 |
| 构建产物 | `frontend/dist` | 正常生成 |

---

## 六、结论

本轮 `g3-main` 在后端单元测试、后端类型检查、前端类型检查和前端生产构建四个单元级质量门禁上全部通过。测试对象与远端最新 `origin/g3-main` 保持一致，结果可作为 P4 阶段单元测试交付记录。
