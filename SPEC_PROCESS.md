# SPEC_PROCESS：规约形成与修订过程

> 本文只记录已有证据。陌生 Agent 冷启动尚未执行，因此对应章节明确保留为未完成，不用推测结果代替客观验证。

## 1. Brainstorming 的关键节点

最初目标来自课程 Project A 与一个已有 NJU-Match 项目。关键问题不是
“能否加一个聊天框”，而是 Harness 的使用者、真实工具、客观反馈、
确认边界，以及 Coding 领域与产品方向是否冲突。

对方向影响最大的追问/检查包括：

1. NJU-Match 的问卷、圈子、论坛和匹配应当被 Agent 替换，还是作为可调用的稳定模块？
2. GitHub CI、安全扫描和测试是产品 Agent 的工具，还是开发流程的保障？
3. 写操作能否只靠提示词要求模型谨慎，还是必须由后端状态机拦截？
4. 独立 `agent-harness` 包存在，是否足以证明真实 WebUI 正在使用它？
5. 课程 A 明确要求 Coding Agent 时，Social Agent 产品方向应如何如实处理？

## 2. 至少三轮关键迭代

### 迭代一：从“代码库安全/测试 Agent”改为面向用户的 Social Agent

原设想把仓库安全检查、测试反馈、CI 和代码修补硬套为 Agent 服务。
用户明确修正：问卷之外还有匹配、同好圈子和论坛交流，这些业务能力
应交给 Agent 编排；Agent 应直接面向最终用户。

处理决策：

- 采纳用户修正，重写 `SPEC.md`、`PLAN.md` 和 Workflow 语义；
- 把资料、问卷、圈子、论坛定义为业务工具；
- 把 CI、CodeQL、Secret Scan 留在工程治理层；
- 明确问卷仍是原有功能，不能被强行改造成聊天 Agent。

### 迭代二：从“推荐/描述”推进为真实可执行动作

在只读检索完成后，用户进一步要求 Agent 能在权限内执行加入圈子申请、圈内 LiveChat、论坛动作、匹配动作和通知动作。

处理决策：

- 只读动作可以直接执行；
- 草稿可以生成但不等同于发布；
- 发帖、评论、加入圈子、发送消息等写操作必须进入服务端确认流程；
- 资源 ID 必须来自当前页面或授权查询结果，不能信任用户文本中的任意 ID；
- 联系方式等敏感输入在确认阶段补齐，不能由模型猜测。

这一轮使“危险动作”从提示词约束变成可离线测试的一次性、用户绑定、资源绑定确认机制。

### 迭代三：从独立 Demo 修正为真实 WebUI 使用 Harness

代码审计发现 `agent-harness` 已有自研循环和 Mock 测试，但真实
`/agent` 服务仍直接调用 Provider 的一次 Responses API。这样只能证明
Demo Harness 存在，不能证明交付 WebUI 使用了该机制。

处理决策：

- 增加后端 Runtime 适配器，把 Provider 限制为“每轮返回一个结构化动作”；
- 由自研 `AgentLoop` 完成解析、分发、Observation 回灌、重试和停止；
- 前端传递稳定的会话 UUID；
- Trace 增加 Harness 状态、步数和实际工具序列；
- 使用 Mock LLM 对同一生产适配路径做无网络测试。

### 迭代四：正面处理 Project A 的 Coding 领域要求

重新逐条核对 Project A 原文后，发现 Social Agent 即使具备完整 Harness 六维机制，仍与“能读写代码、执行命令、运行测试并根据测试自我修正”的字面要求存在领域差异。

处理决策：

- 不把服务器文件/Shell 能力暴露给普通用户；
- 在同一自研内核上增加隔离的 Coding adapter；
- 用确定性代码限制工作区路径、敏感文件、危险命令和确认令牌；
- 把测试退出码转换为 `VALIDATION_FAILED` 并回灌循环；
- 保留“课程方是否接受双轨交付”的未决风险，不用文档措辞冒充确认。

## 3. AI 建议的采纳与推翻

采纳：

- 将业务 Service 置于 Tool Port 后面，避免复制原业务逻辑；
- 使用 Mock LLM 验证反馈、停机、治理和记忆；
- 用服务端认证身份覆盖模型输入；
- 用结构化命令与 `shell: false` 避免 Shell 拼接；
- 将真实 WebUI 接入同一 Harness 内核，并保留可观测 Trace。

推翻或修正：

- 推翻“把 CI/安全扫描当作 Social Agent 工具”的早期方向，因为它不服务最终用户；
- 推翻“独立 Demo 已足以证明应用 Agent 化”的判断，因为生产路径曾绕过循环；
- 修正“Social Agent 完全满足 Project A”的表述，因为课程原文明示 Coding 领域；
- 不接受为满足交付清单而由 AI 代写 `REFLECTION.md`，该文件必须由学生本人完成。

## 4. Brainstorming 方法反思

做得好的部分：连续追问让产品边界、写操作确认和真实执行证据逐步清晰；特别是“移除真实 LLM 后是否仍可测试”的判据，能有效区分提示词与工程机制。

不足：早期方向修正过度强调产品合理性，没有立即逐句对照 Project A 的 Coding 字面要求；此外，历史实现批次没有始终保留严格的红—绿—重构终端证据，后续只能如实标注流程偏差，不能追溯性补造。

## 5. 陌生 Agent 冷启动验证（未完成硬门槛）

截至 2026-08-07，尚未使用不同智能体类型、全新会话和无历史 memory，
只提供 `SPEC.md + PLAN.md` 实施 1–2 个 Task。因此以下要求仍未满足：

- 陌生 Agent 在何处暂停、提出了什么问题；
- 它对 Spec 做出的错误解读；
- 产出与预期的客观差异；
- 因此产生的 SPEC/PLAN 前后 diff。

执行协议：

1. 选择与主开发 Agent 不同的工具/模型类型；
2. 新建无历史会话，不导入本项目 memory；
3. 只提供 `SPEC.md`、`PLAN.md` 和仓库，不补口头解释；
4. 指定 TASK-071 或 TASK-072 中一个尚未由该 Agent 接触的独立子任务；
5. 要求遇到歧义立即暂停提问；
6. 保存问题、实现 diff、测试输出与耗时；
7. 由学生判断是 Spec 缺陷还是 Agent 阅读错误，并据证据更新本节。

统一的执行记录表和验收口径见 [`docs/COLD_START_EVIDENCE.md`](./docs/COLD_START_EVIDENCE.md)。该文件当前仍是待执行协议，不代表冷启动已经完成。

## 6. Superpowers 与 TDD 证据边界

七步工作流映射、可复核红—绿记录和缺失项统一见
[`docs/SUPERPOWERS_TDD_EVIDENCE.md`](./docs/SUPERPOWERS_TDD_EVIDENCE.md)。
当前历史能够证明若干步骤采用了相同工程方法，但不能证明 Superpowers 插件或
对应 skill 曾在所有任务中实际触发。生产 Runtime 等没有独立红灯的批次继续
保留为流程偏差，不进行追溯性补造。
