# REFLECTION（学生本人填写模板）

> 课程要求 1500–2500 字，且禁止 AI 代写。本文件只提供结构与证据提示，
> 不构成可提交的反思正文。学生应删除提示语，并标注 AI 润色范围（如有）。

## 1. 项目与个人判断（建议 150–250 字）

- 为什么选择 Project A？
- 为什么产品方向最终是 Social WebUI + 隔离 Coding adapter？
- 你如何评价这一选择与课程字面要求之间的张力？

## 2. Superpowers 技能的实际作用（建议 250–350 字）

- 哪些技能真正改变了决策或代码？引用 `AGENT_LOG.md` 中具体 Task。
- 哪些步骤形式大于实质？为什么？
- 不要只列技能名称，要写一个你亲自推翻或修正 AI 建议的例子。

## 3. TDD：阻碍还是放大器（建议 250–350 字）

- 使用 coding guardrail 的红灯、绿灯证据；
- 生产 Runtime 适配批次未保存严格先红证据，这暴露了什么流程问题？
- 哪类机制特别适合确定性测试，哪类仍依赖集成环境？

## 4. Spec、Plan 与陌生 Agent 冷启动（建议 250–350 字）

- 完成 `SPEC_PROCESS.md` 冷启动后再写；
- 给出一处规约不清导致暂停或误解的具体案例；
- 展示修订前后关键差异，以及为何新版本更可执行。

## 5. Subagent / Task 粒度与人工评审（建议 200–300 字）

- 哪种 Task 粒度最容易失控？
- 两阶段评审发现了什么 Critical issue？
- 人工修改了哪些内容，为什么不能直接接受 Agent 输出？

## 6. Prompt、Context、凭据与分发（建议 250–350 字）

- 最有效的上下文裁剪策略是什么？
- `.env` 的明文风险、最终安全凭据方案和实际操作证据；
- 本地包依赖为何改变 Docker Context，干净机器验证发现了什么？

## 7. 批判与重做选择（建议 200–300 字）

- Superpowers 假设了什么？这些假设在四天项目中成立吗？
- 如果重做，会更早验证什么？
- 如何避免“文档说执行了，实际生产路径未执行”的问题？

## 提交前自检

- [ ] 正文由学生本人撰写且为 1500–2500 字；
- [ ] 至少引用 3 个真实 Task/PR/测试证据；
- [ ] 明确写出一次人工反对或修正 AI 的判断；
- [ ] 不虚构陌生 Agent 冷启动、TDD 红灯或 CI 结果；
- [ ] 如使用 AI 润色，已标注范围；
- [ ] 已删除本模板中的提示语和复选框。
