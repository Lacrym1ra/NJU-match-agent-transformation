# REFLECTION：从既有校园社交应用到可验证的 AI 原生功能

<!-- markdownlint-disable MD010 MD013 -->

## 1. 项目方向与贡献边界

​	在启动此项目的改造之前原 NJU Match 已有资料、问卷、匹配、圈子、论坛和私信，即使接入 Agent，也仍是继承基线。
​	明确边界后，我新增了具有独立数据表、后端、API、前端和状态机的“共鸣胶囊”与“安心赴约”，并把自研 Harness Agent 作为第三个模块。旧功能为 Agent提供真实上下文；新贡献则必须由权限规则和数据库变化独立证明。工程规模不能代替贡献边界，Agent 的回答文案也不能代替实际执行。

## 2. Superpowers：真正有用与形式化的部分

​	在整个开发过程中，帮助最大的是 `brainstorming`、`test-driven-development` 和完成前验证。Brainstorming 迫使我回答 Agent 面向谁、哪些写操作需要确认、旧项目能否计入新模块。TASK-090–092 中，“旧 NJU Match 不能算新功能”的修正才形成现在的模块边界。TDD 则把“Agent 表示完成”转化为数据库记录、确认前零副作用和一次性 Token 消费。

​	在同样的过程中，形式大于实质的是缺少证据时机械填写七步流程。仓库能证明若干任务采用了相同方法，却不能证明每一步都触发过 Superpowers 插件；`using-git-worktrees` 和`subagent-driven-development` 更不能仅凭文档打勾。因此日志保留缺口，没有用补写说明覆盖历史。方法论的价值应是约束决策和产出证据，而非罗列技能名称。

## 3. TDD 是阻碍还是放大器

​	在契约、状态机和安全边界上，TDD 是放大器。两个新模块先加入 Policy 测试，因实现不存在得到 `ERR_MODULE_NOT_FOUND`，随后才加入最小实现。数据库测试验证双方回答同时揭晓、赴约所有权、未签到不能完成和 Agent 未确认不能写入。最终后端 347 项、前端 34 项、Harness 44 项测试通过，PostgreSQL 3/3、Chromium 6/6 通过。

​	但强制 TDD 不等于单元测试万能。真实浏览器测试发现表单在 `await` 后读取React `event.currentTarget`，后端已写入而页面没有刷新；Mock 未暴露该时序问题。保存表单引用并重建生产前端后才通过。测试层级选错会带来虚假安全感；对 UI、容器和数据库边界，真实集成验证比形式上的每行先红更重要。
这也提醒我，不能用测试数量替代测试是否命中真实风险的判断。

## 4. SPEC、PLAN 与 Task 粒度

​	在初期开发过程中，最严重的规约问题是早期没有写清最终项目类型及“旧功能是否计入贡献”。结果是主开发 Agent 一度围绕 Project A 增加 Coding adapter，并错误认为旧身份、圈子和论坛可满足 Project B 的模块数量。修订后的 SPEC 明确：旧业务不计数；只有共鸣胶囊、安心赴约和 Harness Agent 是本阶段三个模块；入口和联动代码也不能冒充第四个模块。这个案例证明，PLAN 写得再细，也无法弥补上层验收口径错误。

​	需要说明的是，本项目尚未完成异类型陌生 Agent 冷启动，也没有真实的新鲜
subagent 执行记录，所以我不能把上述偏离伪称为“subagent 偏离”，更不能编造
它能自主运行多少分钟。现有证据只能说明主 Agent 在任务边界清楚时可以完成一个“失败测试—最小实现—同层验证”的闭环；范围同时包含产品重定向、前后端、部署和文档时就容易偏离。由此推断，较优 Task 应只有一个可观察目标，限定文件范围，给出失败测试和停止条件，并能在一次会话中完成；但这个结论仍应通过课程要求的异类型冷启动实验补证。

## 5. Prompt、Context 与人工评审

​	在主体的开发构建过程中，最有效的 Prompt 是提供不可改变的业务边界、允许修改的文件、验收命令和禁止宣称的能力。例如 Agent 可读取业务工具结果，却不能传入任意 `userId`；可得到胶囊和赴约状态，却看不到问题正文、地点和备注；写操作先返回 Proposal，再由服务端令牌确认。模型只得到当前决策所需信息，权限仍由确定性代码控制。

​	在此过程中，人工评审的作用是反对错误目标，而不只是修改语法：一是拒绝把 CI/代码安全当作社交 Agent 工具，二是拒绝把继承功能算作新开发。前者改变产品对象，后者促成两个独立模块，说明人必须对“做什么”和“是否完成”负责。

## 6. 凭据与分发带来的工程认识

​	构建凭据时，凭据要求迫使我区分开发便利与生产安全。`.env` 是明文，可能被误提交、备份或通过进程环境暴露。开发环境可使用被 Git 忽略的配置；Ubuntu 生产环境通过隐藏输入创建 systemd encrypted credential，运行时只读挂载，查询不回显 Key，并支持更新和清除。Trace、错误和 Action 也不能保存邀请码或 Key 明文。

​	分发要求说明“本机能运行”不等于可交付。后端依赖仓库内的 `agent-harness`，Docker context 必须来自仓库根；Windows 需要 Linux container mode，Ubuntu还涉及 systemd、Compose、Nginx 和证书。统一测试、健康检查和干净构建是在验证依赖是否被隐式留在开发者机器上。

## 7. 对方法论的批判与重做选择

​	Superpowers 假设需求可较早稳定、任务可切分、测试 Oracle 可定义，且有时间维护 worktree、subagent 和两阶段评审。权限状态机和 Agent Tool 符合这些假设；A/B 方向切换、视觉体验和部署网络却不完全符合。需求根本变化时，继续执行旧PLAN 只会更高效地做错事。

​	如果重做，我会先冻结新旧边界和项目类型，再让陌生 Agent 仅凭 SPEC/PLAN 完成一个小 Task；随后建立真实 PostgreSQL 与浏览器测试，最后扩展 Agent 和部署。每个 Task 保存红、绿、重构、评审和提交引用。Superpowers 是纪律脚手架，但不能替代产品判断，也不能把未发生的协作变成证据。
